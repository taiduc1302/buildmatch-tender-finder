# PORTABLE PACKAGE AUDIT — Tender Finder

Audit of `Tender_Finder_Generic_Portable_Package_20260703_215523`
performed 2026-07-03 after sanitization, smoke testing, and cleanup, and
**independently re-run 2026-07-04** by a separate 8-agent adversarial audit
against a from-scratch re-extraction of the distributable ZIP (see
`FINAL_HANDOFF_AUDIT.md` for that round in full). This file restates the same
underlying findings as `SANITIZATION_REPORT.md`'s "Tests / checks performed"
table — it is a checklist view of the same two audit rounds, not a separate
or different test run. Re-run any time with: `python scripts\package_audit.py`

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Sanitized folder exists | **PASS** | `C:\t\Tender_Finder_Generic_Portable_Package_20260703_215523` |
| 2 | Final ZIP exists | **PASS** | `Tender_Finder_Generic_Portable_Package_20260703_215523.zip` next to the folder; ZIP entry listing re-scanned after creation (see external records for the scan log) |
| 3 | README exists | **PASS** | `README.md` (+ INSTALL, RUNBOOK, PROJECT_STRUCTURE, FUTURE_WEB_APP_PLAN) |
| 4 | Demo workbook exists | **PASS** | `latest_verified_output\demo_synthetic_sample\TENDER_FINDER_DEMO_Opportunities_Three_Buckets.xlsx`, generated by this package from synthetic inputs; regenerate with `run_demo_synthetic.bat` |
| 5 | No original-brand references in user-facing files | **PASS** | `scripts\package_audit.py` case-insensitive scan of 177 text files **and all 6 workbooks' cell text**: 0 hits; filename scan: 0 hits |
| 6 | No real emails except placeholders | **PASS** | Email scan: only `@example.com` placeholders and public procurement-portal/agency contact addresses (public infrastructure, allow-listed and documented) |
| 7 | No obvious secrets | **PASS** | Pattern scan (Tavily/Anthropic/OpenAI/AWS/GitHub/Slack keys, private keys, password/token assignments, cookies): 0 hits; the only key-like string is the documented placeholder `tvly-your-key-here` in `.env.tenderfinder.local.example` |
| 8 | No `.git`, `.venv`, `__pycache__`, cache folders | **PASS** | Excluded at build; runtime `__pycache__`/logs from smoke tests deleted before zipping; audit script flags them if they reappear. Verified 2026-07-04 by extracting the actual ZIP into a brand-new, never-touched folder and confirming zero such folders before running anything in it — this rules out any doubt from cache folders that naturally appear once you *run* the tool (see `RUNBOOK.md` troubleshooting). |
| 9 | Production outputs not copied unsanitized | **PASS** | All 15+ real output folders excluded; the only shipped output is the synthetic sample generated by this package (see `latest_verified_output\demo_synthetic_sample\README_SYNTHETIC.md`) |
| 10 | Code entry points documented | **PASS** | `PROJECT_STRUCTURE.md` (module table + launcher table + data-flow diagram), `RUNBOOK.md` (all run modes) |
| 11 | Manual review items listed | **PASS** | `SANITIZATION_REPORT.md` §Manual review items (6 items: public-record test fixtures, PDF fixtures, public brand tables, BC source registers, historical docs, provider domains in demo emails) |

## Functional verification

(Same results and wording as `SANITIZATION_REPORT.md`'s "Tests / checks
performed" table — repeated here as a quick checklist, not a second run.)

| Check | Result |
|---|---|
| `verify_package.bat` | **PASS** (`VERIFY_PACKAGE: PASS`) — reconfirmed 2026-07-04 from a fresh ZIP extraction |
| Offline synthetic demo build | **PASS** (workbook + talktrack + build report + summary generated) |
| Anti-fixture guard on synthetic data | **PASS (guard correctly triggered)** — reports FAIL by design; this is the safety feature working, not a defect. Full explanation: `TEST_RESULTS.md` |
| Standalone tests | 21/23 pass; 2 known, explained, non-blocking failures (see `TEST_RESULTS.md`) |
| Live network fetching | **NOT re-tested** from this package (honest limitation, not hidden) |

## Audit environment

- Round 1 (2026-07-03): Windows 11, Python 3.14.6, openpyxl 3.1.5. Automated
  build-time classification (≈250 inventory rows, external CSV) + post-build
  `scripts\package_audit.py` + independent grep sweeps for brand tokens,
  personnel names, `OneDrive`, `C:\Users\`, and email addresses.
- Round 2 (2026-07-04): independent 8-agent adversarial workflow against a
  from-scratch ZIP re-extraction — fresh scan, fresh demo run in a fresh venv,
  two independent "new reader" documentation reviews, and two skeptic passes
  that actively tried to disprove round 1's clean result. Full trace and the
  one process lesson it surfaced (test-execution artifacts vs. actual ZIP
  contents) are in `FINAL_HANDOFF_AUDIT.md`.

## Verdict

**SAFE TO KEEP INTERNALLY / AS A PRIVATE ARCHIVE — right now, no further
action needed.**

**SAFE TO HAND TO A DEVELOPER — after a quick human read of the "Manual
review items" in `SANITIZATION_REPORT.md`** (public-record test fixtures, a
BC-specific source list, and similar already-public content that a reviewer
should sign off on before it leaves your organization).

**NOT YET CLEARED FOR FULLY PUBLIC RELEASE** (open internet, public repo, etc.)
until those same manual-review items get an explicit legal/IP sign-off — see
`DISTRIBUTION_NOTES.md` for exactly what that means file-by-file.

# Runtime and repository test dependencies discovered from Python imports.
openpyxl>=3.1.5
beautifulsoup4>=4.15.0
playwright>=1.62.0
pandas>=3.0.5
pdfplumber>=0.11.10
reportlab>=5.0.1
requests>=2.34.2
urllib3>=2.7.0
pyyaml>=6.0.3
regex>=2026.9.3
# BuildMatch Tender Finder AI Opportunity Copilot (Build Week): OpenAI Responses
# API client for tenderfinder_ai_analysis.py. Only imported when a live analysis
# is requested (OPENAI_API_KEY set); the mocked test suite and all deterministic
# features work without it, but a normal install should still provide it.
openai>=3.8.0

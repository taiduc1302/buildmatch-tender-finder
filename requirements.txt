# Runtime and repository test dependencies discovered from Python imports.
openpyxl>=3.1.5
beautifulsoup4>=4.15.0
playwright>=1.63.0
pandas>=2.0
pdfplumber>=0.11.10
reportlab>=4.0
requests>=2.34.2
urllib3>=2.8.0
pyyaml>=6.0.3
regex>=2026.9.10
# BuildMatch Tender Finder AI Opportunity Copilot (Build Week): OpenAI Responses
# API client for tenderfinder_ai_analysis.py. Responses API support was added in
# openai-python 1.66.0; the mocked test suite and deterministic features work
# without a live API key, but a normal install should provide this SDK surface.
openai>=1.66.0
